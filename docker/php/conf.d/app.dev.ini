; Dev overrides
memory_limit=512M

; Validate timestamps so code changes are picked up in dev
opcache.validate_timestamps=1
opcache.revalidate_freq=0

; Xdebug — coverage mode only (not step debugging by default)
xdebug.mode=coverage
